<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.imaginecode.imaginecode">

    <!-- Permission: Allow Connect to Internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Permission: Writing to SDCard -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-feature android:name="android.hardware.usb.host" />
    <uses-permission android:name="android.permission.USB_PERMISSION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:windowSoftInputMode="stateHidden|adjustPan">






        <activity
            android:name=".LaunchScreenActivity"
            android:label="ImagineCode"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CheckIn"
            android:label="@string/title_activity_check_in"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            </intent-filter>
        </activity>
        <activity
            android:name=".IntroModActivity"
            android:label="@string/title_activity_levels"
            android:theme="@style/AppTheme.NoActionBar">



        </activity>

        <activity
            android:name=".ModulesActivity"
            android:label="@string/title_activity_modules"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>

        <activity
            android:name=".AddStudent"
            android:label="@string/title_activity_add_student"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".BlocklyLessonActivity"
            android:theme="@style/AppTheme">





        </activity>

        <activity android:name=".LanguageSelection" android:theme="@style/AppTheme.NoActionBar">

        </activity>

    </application>





</manifest>