<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="BlocklyVerticalTheme">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />


    <style name="AppTheme.CustomToolBar" parent="ThemeOverlay.AppCompat.Dark.ActionBar" >
        <item name="android:textColor">#ecf0f1</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="EditTextTheme" >
        <item name="colorControlNormal">#f1c40f</item>
        <item name="colorAccent">#f1c40f</item>
        <item name="colorControlActivated">#f1c40f</item>
        <item name="colorControlHighlight">#f1c40f</item>
        <item name="android:textSize">15sp</item>
        <item name="android:textColor">@color/white</item>

    </style>

    <style name="RatingBar" parent="ThemeOverlay.AppCompat">
        <item name="colorControlNormal">@color/headbar</item>
        <item name="colorControlActivated">@color/orange</item>
        <item name="colorAccent">@color/orange</item>

    </style>


</resources>
